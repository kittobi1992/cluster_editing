
#include "instance.h"

#include <optional>

std::optional<Instance> thomas(Instance graph);

std::optional<Instance> thomas_pairs(Instance graph);

std::optional<Instance> heavy_edge_single_end(Instance inst);

std::optional<Instance> heavy_non_edge_single_end(Instance inst);
